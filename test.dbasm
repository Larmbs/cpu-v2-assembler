
section .data
    ; Defining constants
    numbers: db 5, 10
    
section .text
    ; Writing the program 

    global _start

    _start:
        LDR ra, numbers
        LDR rb, numbers + 2
        ADD _, ra, rbb 5, 10
    